
This DecSoft HTML Compiler sample HTML app shows how we can use
the dhc.windows.getSize() and dhc.windows.setSize() to get and set 
the app window size, so we can, for example, to save the window size
when the app is closed, and, set that size when the app is started.

