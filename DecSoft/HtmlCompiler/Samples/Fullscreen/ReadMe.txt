
This DecSoft HTML Compiler sample HTML app shows how we can toggle to
fullscreen in runtime, by using the provided external Javascript functions.

We can start our app in fullscreen by setting the Window app options,
but using the referred external function we can do it also at runtime.
