
This DecSoft HTML Compiler sample HTML app show how we can save a blob
into a file using the dhc.base64.decodeToFile() function. 

This function is intended mainly to save a base64 string into a file, after 
decode it, that is, it's intended to save Javascript blob objects into files.
